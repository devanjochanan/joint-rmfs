# RMFS Input Data Placeholder

This folder is reserved for a future input-data relocation phase.

No baseline CSVs were moved here in Phase 3. Files such as root-level pod, SKU, item, and order CSVs remain the current source of truth for active runs.

Moving inputs into this folder later requires explicit path-handling updates in Python and any NetLogo-facing workflow that reads those files.
